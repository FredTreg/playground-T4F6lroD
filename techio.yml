tittle : Python template by FredTreg

# Tab    adsfgn:
  -        title: Welcome # Set the page title
 qsdf
# Settings for your programming projects and the associated Docker images
projects:
          python:
    root: /python-project # Set the root path to your Python project
gq # @[Code Editor Title]({"stubs": ["editor_file1.py", "editor_file2.py", ...], "command": "python3 test_file.py"})
         runner: python:3
